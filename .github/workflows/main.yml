name: scp files
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: copy files via ssh password
      uses: appleboy/scp-action@master
      with:
        host: ${{ secrets.shiny_server }}
        port: ${{ secrets.shiny_port }}
        key: ${{ secrets.SSH_KEY }}
        source: "*"
        target: "/srv/shiny-server/"
